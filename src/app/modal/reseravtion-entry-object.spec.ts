import { ReseravtionEntryObject } from './reseravtion-entry-object';

describe('ReseravtionEntryObject', () => {
  it('should create an instance', () => {
    expect(new ReseravtionEntryObject()).toBeTruthy();
  });
});
